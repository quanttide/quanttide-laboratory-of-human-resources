# AGENTS.md - 实验室工作指南

## 核心理解

### 「自研」的定义

**自研 = 让邮件从流程里消失。**

当前招聘流程依赖邮件作为核心通信渠道：
- 候选人投递 → 邮件
- 发送问卷 → 邮件
- 发送邀请 → 邮件
- 发送考核 → 邮件
- 归档管理 → 邮件文件夹

自研的目标是**完全移除邮件依赖**，所有流程走自研系统：
- 候选人投递 → 自研系统表单
- 发送问卷 → 系统内通知（站内信/消息推送）
- 发送邀请 → 系统内通知
- 发送考核 → 系统内通知
- 归档管理 → 数据库状态流转

### 为什么邮件要消失

1. **邮件是外部依赖**：依赖第三方邮件服务（飞书/SMTP），不可控
2. **邮件是异步黑盒**：无法追踪候选人是否阅读、何时阅读
3. **邮件是流程断点**：邮件内外是两个世界，数据无法自动关联
4. **邮件是管理负担**：需要维护文件夹结构、归档逻辑、发送日志

### 自研系统的特征

1. **单一生效源**：所有数据在自研系统内产生、流转、沉淀
2. **状态驱动**：候选人状态在数据库中显式管理，不依赖邮件文件夹
3. **实时可追踪**：每一步操作都有记录，可审计、可回溯
4. **API 优先**：所有功能通过 API 暴露，CLI/Web/移动端共用同一后端

## 迁移路径

### 当前架构（邮件驱动）

```
候选人 → 邮件投递 → lark-cli 处理 → 飞书多维表格存储
         ↓
    邮件通知（问卷/邀请/考核）
         ↓
    邮件归档（文件夹管理）
```

### 目标架构（系统驱动）

```
候选人 → 自研系统表单 → 数据库存储
         ↓
    系统内通知（站内信/推送）
         ↓
    状态流转（数据库字段）
```

### 关键改动

| 当前 | 目标 |
|------|------|
| lark-cli 发送邮件 | 自研系统发送通知 |
| 飞书多维表格存储 | PostgreSQL/MySQL 存储 |
| 邮件文件夹归档 | 数据库状态字段 |
| 邮件模板渲染 | 系统内消息模板 |
| 邮件发送日志 | 操作审计日志 |

## 技术选型

### 后端服务

- **语言**：Go（与 Provider 一致）
- **框架**：Gin/Echo（轻量级 HTTP 框架）
- **数据库**：PostgreSQL（支持 JSON 字段，适合灵活数据模型）
- **缓存**：Redis（会话管理、消息队列）

### 前端

- **框架**：React/Vue（与 Studio 一致）
- **UI 库**：Ant Design/Element Plus（管理后台常用）

### 通知渠道

- **站内信**：系统内消息中心
- **推送**：WebSocket 实时推送
- **短信**：可选，重要通知备用渠道

## 开发原则

1. **先数据后界面**：先设计数据模型和 API，再做前端
2. **先单个后批量**：先实现单个候选人流程，再做批量操作
3. **先核心后边缘**：先实现投递→问卷→邀请→考核主流程，再做统计报表
4. **先本地后云端**：先在本地跑通，再部署到服务器

## 验证清单

- [ ] 候选人可以通过系统表单投递
- [ ] 管理员可以在系统内查看投递记录
- [ ] 管理员可以发送问卷通知（站内信）
- [ ] 候选人可以在系统内查看通知
- [ ] 管理员可以发送邀请通知
- [ ] 管理员可以发送考核通知
- [ ] 所有操作都有审计日志
- [ ] 无任何邮件依赖
